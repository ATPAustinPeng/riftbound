diff --git a/node_modules/@shopify/flash-list/dist/recyclerview/RecyclerView.js b/node_modules/@shopify/flash-list/dist/recyclerview/RecyclerView.js
index 8bea626..93766b4 100644
--- a/node_modules/@shopify/flash-list/dist/recyclerview/RecyclerView.js
+++ b/node_modules/@shopify/flash-list/dist/recyclerview/RecyclerView.js
@@ -224,7 +224,14 @@ var RecyclerViewComponent = function (props, ref) {
      */
     var validateItemSize = (0, react_1.useCallback)(function (index, size) {
         var _a, _b, _c, _d;
-        var layout = recyclerViewManager.getLayout(index);
+        var layout = recyclerViewManager.tryGetLayout(index);
+        if (layout === undefined) {
+            // Stale ViewHolder onLayout firing for an index that no longer
+            // exists in the layout table (e.g. list shrank from a fast
+            // filter/search). Safe to ignore. See:
+            // https://github.com/Shopify/flash-list/issues/2291
+            return;
+        }
         var width = Math.max(Math.min(layout.width, (_a = layout.maxWidth) !== null && _a !== void 0 ? _a : Infinity), (_b = layout.minWidth) !== null && _b !== void 0 ? _b : 0);
         var height = Math.max(Math.min(layout.height, (_c = layout.maxHeight) !== null && _c !== void 0 ? _c : Infinity), (_d = layout.minHeight) !== null && _d !== void 0 ? _d : 0);
         if ((0, measureLayout_1.areDimensionsNotEqual)(width, size.width) ||
