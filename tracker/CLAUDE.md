@AGENTS.md

# Workflow

- Whenever you make changes to files (code, config, docs), always end your response with a suggested commit message for those changes, even if you don't run `git commit` yourself.
